using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BufferBoundary : MonoBehaviour
{
    //public CameraBuffer camBuffer;

    private void OnTriggerEnter(Collider other)
    {

        //Vector3 currPos = camBuffer.transform.position;
        //Vector3 newPos = Vector3.Lerp(camBuffer.transform.position, other.transform.position, camBuffer.bufferSpeed);
        //camBuffer.transform.position = newPos;
    }
}
